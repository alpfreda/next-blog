import SetupForm from '@/components/setup/form-builder'

const Setup: React.FC = async () => {
  return (
    <section className='setup'>
      <SetupForm />
    </section>
  )
}

export default Setup